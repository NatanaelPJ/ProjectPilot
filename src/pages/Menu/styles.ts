import styled from "styled-components";

export const ContentMenu = styled.article`
  flex: 1;
  display: flex;
  flex-direction: column;
  padding: 1em 1em 0;

  .LoadMore {
    margin: 2em auto;
    max-width: 11em;
    width: 100%;
    padding: 1em;
    background: black;
    color: white;
  }
`